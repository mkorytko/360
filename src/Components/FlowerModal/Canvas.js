import React from 'react';

const Canvas = () => (
    <canvas
        className="modal-window-image_canvas"
        id="canvas"
        width="100px"
        height="110px" />
);

export default Canvas;
